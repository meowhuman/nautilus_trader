[project]
name = "claude-hooks"
version = "0.1.0"
description = "Claude Code hooks and utilities for project automation"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    "elevenlabs>=2.20.0",
    "requests>=2.30.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
